<!DOCTYPE html>
<html>
  <%- include('../partials/head', {title}) %>
  <body class="grey lighten-4">
    <%- include('../partials/header', {title}) %>
    <main>
      <%- include('../partials/sidenav') %>

      <!---->
      <% if (locals.errors) { %><!---->
      <%- include('../partials/errors', {errors}) %><!---->
      <% } %>

      <div class="row">
        <div class="col s12 center-align">
          <h3><%= title %></h3>
        </div>
      </div>

      <div class="row">
        <div class="col s12 red lighten-5 red-text">
          <h2 class="center-align"><%= category.name %></h2>
        </div>

        <div class="col s12 red lighten-5 red-text">
          <p class="center-align">
            There are <%= count %> item(s) belonging to this category.
          </p>
        </div>

        <div class="col s12 center-align">
          <form action="" method="post">
            <div class="input-field col s12">
              <label for="username">Username</label>
              <input
                class="validate"
                required
                id="username"
                name="username"
                type="text"
                minlength="3"
              />
            </div>
            <div class="input-field col s12">
              <label for="password">Password</label>
              <input
                type="password"
                required
                name="password"
                minlength="3"
                class="validate"
              />
            </div>
            <input
              type="text"
              hidden
              disabled
              value="<%= category._id %>"
              name="id"
            />
            <button type="submit" class="btn red">Delete</button>
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
